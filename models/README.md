# Models Directory

This directory contains saved model weights:

- `vit_best.pt` - Best Vision Transformer model
- `vgg16_baseline_best.keras` - Best VGG16 baseline model

Note: Model files are not tracked in git due to their size.
